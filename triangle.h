#ifndef TRIANGLE_H
#define TRIANGLE_H
#include <vector>
using namespace std;

double lengthOfSide(double, double, double, double);
double triangleArea(double, double, double);
double minArea(vector<double> areaArray);
double maxArea(vector<double> areaArray);

#endif